services:
  - type: web
    name: fishtank-monitor
    env: php
    buildCommand: ""
    startCommand: "php-fpm -D && nginx -g 'daemon off;'"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fishtank_monitor
          property: connectionString